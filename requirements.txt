# Runtime dependencies for biasguard.
# For development, install the project with extras instead:  pip install -e ".[dev]"
numpy>=1.24
pandas>=2.0
pyarrow>=12.0
scipy>=1.10

# Reporting (optional at runtime, required for HTML reports):
plotly>=5.18
jinja2>=3.1
